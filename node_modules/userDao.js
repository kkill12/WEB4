var baseDao = require('baseDao');
var util = require('util');
function userDao(){
	//初始化表名、主键名、SQL语句
	baseDao.call(this);
	this.fun['setTableName']('pms_users');
	this.fun['setPrimarykey']('userid');
	//this.fun['setConn']('setConn');
};
module.exports = userDao;

//继承
util.inherits(userDao,baseDao);